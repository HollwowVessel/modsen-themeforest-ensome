import { ComponentMeta } from '@storybook/react';
import { HelpSection } from './index';

export default {
  title: 'Section/Contacts/HelpSection',
  component: HelpSection,
} as ComponentMeta<typeof HelpSection>;

export const Example = <HelpSection />;
